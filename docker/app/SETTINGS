
# META

SITE_TITLE = 'nebula'


# SECURITY

SECRET_KEY = 'changeme'
SSH_SECRET = 'changeme'


# CACHE

CACHE_ROOT = "/tmp/cache"


# CELERY

CELERY_BROKER_URL = "amqp://rabbitmq:5672"
CELERY_RESULT_URL = False


# DATABASE

DB = 'nebula'
DB_USERNAME = 'nebula'
DB_PASSWORD = 'nebula'
DB_HOST = 'database'


# AWS

REGION_NAME = 'us-east-1'
EBS_OPTIMIZED = False
SUBNET_ID = 'subnet-1fb84b55'
SECURITY_GROUP_ID = 'sg-7949c70f'
# IAM_INSTANCE_PROFILE



# LDAP

LDAP_PROVIDER_URL = 'ldap://ldap'
LDAP_BIND_DN = 'cn=admin,dc=example,dc=org'
LDAP_BIND_PASSWORD = 'admin'

LDAP_USERNAME_ATTRIBUTE = 'cn'
LDAP_USER_BASE = 'ou=users,dc=example,dc=org'

LDAP_GROUP_OBJECT_CLASS = 'posixGroup'
LDAP_GROUP_ATTRIBUTE = 'cn'
LDAP_GROUP_BASE = 'ou=groups,dc=example,dc=org'

LDAP_BANNED_GROUP = 'banned'
LDAP_ADMIN_GROUP = 'nebula_admins'
LDAP_USER_GROUP = 'nebula_users'


# SMTP

# SMTP_DOMAIN =
# SMTP_USERNAME =
# SMTP_PASSWORD =
# SMTP_ORIGIN =
# NOTIFICATION_THRESHOLD =
